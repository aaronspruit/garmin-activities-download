services:
  garmin-sync:
    build:
      context: .
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    container_name: garmin-sync
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./tokens:/app/tokens
